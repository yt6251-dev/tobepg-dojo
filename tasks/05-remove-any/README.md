# 05 index.ts から `any` を1つ消す（証拠つき課題）

AI: **ペア**
目安: 2時間

## 課題

元町Cal の `firebase-app/functions/src/index.ts`（6,118行）から `any` を1か所選んで 正しい型に置き換える。

1. `grep -n "any" index.ts` で候補を出し **権限かデータ整合性に関わる**ものを1つ選ぶ
2. 置き換える前に `testcode/` の該当テストが通ることを確認する
3. 置き換えて `tsc` を通す。テストも通す
4. ブランチ `dojo/05-remove-any` にコミットする

## 合格条件

`npm run grade -- 05 --evidence "<コミットハッシュ>" --note "何を何に変えたか" --explain "なぜその型が正しいと言えるか 3文以上"`

## なぜこれか

第2〜4週の P3（手で書き直す）と P5（認証の後付け）の前哨。自分の製品の型を1つ締めることで 6,118行が「読める」から「直せる」に変わる最初の一歩。
